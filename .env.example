JWT_SECRET=change-me-to-a-long-random-secret
ADMIN_API_KEY=change-me-to-a-long-random-admin-key

# Optional overrides — sensible defaults are set in fmcg_reco.config.Settings
# ACCESS_TOKEN_EXPIRE_MINUTES=30
# REFRESH_TOKEN_EXPIRE_MINUTES=10080
# DATABASE_URL=sqlite:///auth.db
# HYBRID_ARTIFACT_DIR=artifacts/latest/hybrid
# AFFINITY_ARTIFACT_DIR=artifacts/latest/affinity
